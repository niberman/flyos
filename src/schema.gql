# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""An aircraft in the flight school fleet."""
type Aircraft {
  """
  Current airworthiness status. Automatically set to GROUNDED by the predictive maintenance engine when telemetry thresholds are violated.
  """
  airworthinessStatus: AirworthinessStatus!

  """Timestamp when the aircraft record was created."""
  createdAt: DateTime!

  """The aircraft home base."""
  homeBase: Base!

  """UUID of the default home base for this aircraft."""
  homeBaseId: ID!

  """Unique identifier (UUID) for the aircraft."""
  id: ID!

  """Aircraft manufacturer (e.g., Cessna, Piper)."""
  make: String!

  """Aircraft model (e.g., 172 Skyhawk, PA-28 Cherokee)."""
  model: String!

  """UUID of the organization this aircraft belongs to."""
  organizationId: ID!

  """FAA tail number uniquely identifying the aircraft (e.g., N12345)."""
  tailNumber: String!

  """Timestamp of the last update to the aircraft record."""
  updatedAt: DateTime!
}

"""Indicates whether an aircraft is cleared for flight operations."""
enum AirworthinessStatus {
  FLIGHT_READY
  GROUNDED
}

"""
A telemetry threshold violation for an aircraft, for operator visibility.
"""
type Alert {
  """Aircraft that recorded the violation."""
  aircraftId: ID!

  """Tail number of the aircraft."""
  aircraftTailNumber: String!

  """Sensor parameter that violated a threshold."""
  parameter: String!

  """Threshold limit that was exceeded (min or max as applicable)."""
  threshold: Float!

  """Telemetry record timestamp."""
  timestamp: DateTime!

  """Recorded sensor value."""
  value: Float!
}

"""Response returned after successful authentication."""
type AuthResponse {
  """Signed JWT token for authenticating subsequent requests."""
  access_token: String!

  """UUID of the organization the authenticated user belongs to."""
  organizationId: String!
}

"""A physical airport or flight base operated by an organization."""
type Base {
  """Timestamp when the base was created."""
  createdAt: DateTime!

  """ICAO airport code (e.g., KAPA, KBJC)."""
  icaoCode: String!

  """Unique identifier (UUID) for the base."""
  id: ID!

  """Name of the base."""
  name: String!

  """UUID of the organization this base belongs to."""
  organizationId: String!

  """IANA timezone identifier (e.g., America/Denver)."""
  timezone: String!

  """Timestamp of the last update to the base record."""
  updatedAt: DateTime!
}

"""Input for batch uploading maintenance log records."""
input BatchMaintenanceInput {
  """Array of maintenance log entries to ingest."""
  entries: [MaintenanceLogEntry!]!
}

"""Input for batch uploading telemetry records."""
input BatchTelemetryInput {
  """Array of telemetry data entries to ingest."""
  entries: [TelemetryEntry!]!
}

"""A scheduled flight booking for a user and aircraft."""
type Booking {
  """The aircraft reserved for this booking (populated via relation)."""
  aircraft: Aircraft

  """UUID of the booked aircraft."""
  aircraftId: String!

  """The base where this flight originates (populated via relation)."""
  base: Base

  """UUID of the base where the flight originates."""
  baseId: ID!

  """Timestamp when the booking was created."""
  createdAt: DateTime!

  """End time of the booked flight block."""
  endTime: DateTime!

  """Unique identifier (UUID) for the booking."""
  id: ID!

  """Start time of the booked flight block."""
  startTime: DateTime!

  """The user who owns this booking (populated via relation)."""
  user: User

  """UUID of the user who made the booking."""
  userId: String!
}

"""Input for adding a new aircraft to the fleet."""
input CreateAircraftInput {
  """UUID of the default home base (must belong to your organization)."""
  homeBaseId: ID!

  """Aircraft manufacturer."""
  make: String!

  """Aircraft model designation."""
  model: String!

  """FAA tail number (e.g., N12345)."""
  tailNumber: String!
}

"""Input for creating a new flight booking."""
input CreateBookingInput {
  """UUID of the aircraft to book."""
  aircraftId: ID!

  """UUID of the base where the flight originates."""
  baseId: ID!

  """End time of the booking (ISO 8601 format)."""
  endTime: DateTime!

  """Start time of the booking (ISO 8601 format)."""
  startTime: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

"""Input for authenticating an existing user."""
input LoginInput {
  """Email address of the account."""
  email: String!

  """Account password."""
  password: String!
}

"""A maintenance log entry linked to a specific aircraft."""
type MaintenanceLog {
  """UUID of the associated aircraft."""
  aircraftId: String!

  """Raw JSONB maintenance log data as received from batch upload."""
  data: JSON!

  """Unique identifier for the maintenance log."""
  id: ID!

  """Timestamp of the maintenance log entry."""
  timestamp: DateTime!
}

"""
A single maintenance log entry within a batch upload. Aircraft must belong to the caller organization.
"""
input MaintenanceLogEntry {
  """UUID of the target aircraft."""
  aircraftId: String!

  """Raw maintenance log data (arbitrary JSON structure)."""
  data: JSON!

  """Timestamp of the log entry. Defaults to now if omitted."""
  timestamp: DateTime
}

type Mutation {
  """
  Cancel a booking. Allowed for the booking owner or any DISPATCHER in the same organization.
  """
  cancelBooking(bookingId: String!): Boolean!

  """Add a new aircraft to the fleet. DISPATCHER only."""
  createAircraft(input: CreateAircraftInput!): Aircraft!

  """
  Create a flight booking. Fails if aircraft is grounded or time conflicts exist.
  """
  createBooking(input: CreateBookingInput!): Booking!

  """
  Batch upload maintenance log records. INSTRUCTOR and DISPATCHER only. Scoped to your organization.
  """
  ingestMaintenanceLogs(input: BatchMaintenanceInput!): [MaintenanceLog!]!

  """
  Batch upload telemetry sensor data. INSTRUCTOR and DISPATCHER only. Scoped to your organization.
  """
  ingestTelemetry(input: BatchTelemetryInput!): [Telemetry!]!

  """Authenticate with email and password to receive a JWT."""
  login(input: LoginInput!): AuthResponse!

  """Register a new user and receive a JWT access token."""
  register(input: RegisterInput!): AuthResponse!

  """Update aircraft airworthiness status. INSTRUCTOR and DISPATCHER only."""
  updateAircraftStatus(id: ID!, status: AirworthinessStatus!): Aircraft!
}

type Query {
  """List all aircraft in the fleet."""
  aircraft(baseId: ID): [Aircraft!]!

  """
  List aircraft at a base: home-based there or with a booking at that base.
  """
  aircraftByBase(baseId: ID!): [Aircraft!]!

  """
  Telemetry threshold violations for the current organization. Defaults to the last 24 hours.
  """
  alertHistory(aircraftId: ID, hours: Int = 24): [Alert!]!

  """List all bookings in the organization. INSTRUCTOR and DISPATCHER only."""
  bookings(baseId: String): [Booking!]!

  """Bookings for a specific aircraft, optionally filtered by date range."""
  bookingsByAircraft(aircraftId: String!, endDate: DateTime, startDate: DateTime): [Booking!]!

  """Bookings at a specific base, optionally filtered by date range."""
  bookingsByBase(baseId: String!, endDate: DateTime, startDate: DateTime): [Booking!]!

  """Get the profile of the currently authenticated user."""
  me: User!

  """List bookings for the currently authenticated user."""
  myBookings(baseId: String): [Booking!]!

  """List all users. Restricted to INSTRUCTOR and DISPATCHER."""
  users: [User!]!
}

"""Input for registering a new user account."""
input RegisterInput {
  """Unique email address for the account."""
  email: String!

  """
  UUID of an existing organization. If set, the user joins this org (no new org is created).
  """
  organizationId: String

  """
  Name for a new organization. Ignored if organizationId is provided. Creates org, default base, and assigns the user.
  """
  organizationName: String

  """Password (minimum 8 characters)."""
  password: String!

  """Role assigned to the user. Defaults to STUDENT if not specified."""
  role: Role = STUDENT
}

"""User role determining access level within the system."""
enum Role {
  DISPATCHER
  INSTRUCTOR
  STUDENT
}

type Subscription {
  bookingUpdated: Booking!
}

"""A telemetry data point containing sensor readings for an aircraft."""
type Telemetry {
  """UUID of the associated aircraft."""
  aircraftId: String!

  """
  Raw JSONB sensor data (e.g., { oilPressure: 45, cylinderHeadTemperature: 380 }).
  """
  data: JSON!

  """Unique identifier for the telemetry record."""
  id: ID!

  """Timestamp when the sensor data was recorded."""
  timestamp: DateTime!
}

"""
A single telemetry data entry within a batch upload. Aircraft must belong to the caller organization.
"""
input TelemetryEntry {
  """UUID of the target aircraft."""
  aircraftId: String!

  """
  Raw sensor data (e.g., { oilPressure: 45, cylinderHeadTemperature: 380 }).
  """
  data: JSON!

  """Timestamp of the sensor reading. Defaults to now if omitted."""
  timestamp: DateTime
}

"""A registered user in the FlyOS system."""
type User {
  """Timestamp when the user was created."""
  createdAt: DateTime!

  """Email address of the user."""
  email: String!

  """Unique identifier (UUID) for the user."""
  id: ID!

  """UUID of the organization this user belongs to."""
  organizationId: String!

  """Role determining the user's access level."""
  role: Role!

  """Timestamp of the last update to the user record."""
  updatedAt: DateTime!
}